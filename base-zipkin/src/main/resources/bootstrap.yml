spring:
  application:
    name: zipkin
#  profiles:
#    active: development
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:15672
        username: guest
        password: guest

encrypt:
  failOnError: false

